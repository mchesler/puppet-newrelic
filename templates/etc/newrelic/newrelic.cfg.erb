#
# THIS FILE AUTO-GENERATED BY PUPPET
#
license_key=<%= daemon_license_key %>
loglevel=<%= daemon_loglevel %>
logfile=<%= daemon_logfile %>

# The rest of the daemon options are shown with their default values.
# Uncomment and edit to change a setting from its default value.

<% if ! @daemon_collector_host%>#<% end %>collector_host=<%= daemon_collector_host %>
<% if ! @daemon_ssl %>#<% end %>ssl=<%= daemon_ssl %>
<% if ! @daemon_timeout %>#<% end %>timeout=<%= daemon_timeout %>
<% if ! @daemon_socket %>#<% end %>socket=<%= daemon_socket %>
<% if ! @daemon_max_threads %>#<% end %>max_threads=<%= daemon_max_threads %>
<% if ! @daemon_metric_limit %>#<% end %>metric_limit=<%= daemon_metric_limit %>
<% if ! @daemon_special %>#<% end %>special=<%= daemon_special %>
<% if ! @daemon_proxy %>#<% end %>proxy=<%= daemon_proxy %>
